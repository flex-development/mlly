/**
 * @file Unit Tests - PATTERN_CHARACTER
 * @module mlly/utils/tests/unit/PATTERN_CHARACTER
 */

import TEST_SUBJECT from '../pattern-character'

describe('unit:utils/PATTERN_CHARACTER', () => {
  it('should equal "*"', () => {
    expect(TEST_SUBJECT).to.equal('*')
  })
})
