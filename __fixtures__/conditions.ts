/**
 * @file Fixtures - conditions
 * @module fixtures/conditions
 */

import type { Condition } from '@flex-development/pkg-types'

/**
 * User conditions.
 *
 * @const {Set<Condition>} conditions
 */
const conditions: Set<Condition> = new Set<Condition>(['mlly'])

export default conditions
