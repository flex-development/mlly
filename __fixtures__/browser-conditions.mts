/**
 * @file Fixtures - browserConditions
 * @module fixtures/browserConditions
 */

import type { Condition } from '@flex-development/mlly'

/**
 * User conditions for the browser.
 *
 * @const {Condition[]} browserConditions
 */
const browserConditions: Condition[] = ['browser']

export default browserConditions
