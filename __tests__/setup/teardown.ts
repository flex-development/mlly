/**
 * @file Test Setup - Global Teardown
 * @module tests/setup/teardown
 * @see https://vitest.dev/config/#globalsetup
 */

/**
 * Cleanups the global test environment.
 *
 * @return {void} Nothing when complete
 */
export function teardown(): void {
  return
}
