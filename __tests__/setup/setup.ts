/**
 * @file Test Setup - Global Setup
 * @module tests/setup/setup
 * @see https://vitest.dev/config/#globalsetup
 */

/**
 * Prepares the global test environment.
 *
 * @return {void} Nothing when complete
 */
export function setup(): void {
  return
}
